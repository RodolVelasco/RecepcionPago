{% extends "BenDoctorsBundle::layout.html.twig" %}
{% block title %}
Línea de trabajo edit | {{ parent() }}
{% endblock %}
{% block body %}

<h3><span class="glyphicon glyphicon-tag"></span> Editar línea de trabajo</h3>

<form action="{{ path('linea_trabajo_update', { 'id': entity.id }) }}" class="form-horizontal col-md-8" method="post" {{ form_enctype(form) }}>
    {% include "BenDoctorsBundle:LineaTrabajo:form.html.twig" %}
    <div class="form-group">
      <div class="col-md-offset-5 col-md-7">
        <button type="submit" name="subvalider" class="btn btn-primary"><span class="glyphicon glyphicon-edit"></span> Modificar</button>
        <a href="#" id="js-delete" class="btn btn-danger"><span class="glyphicon glyphicon-trash"></span> Eliminar</a>
        <a href="{{ path('linea_trabajo') }}" class="btn btn-success"><span class="glyphicon glyphicon-ok"></span> Retornar a ver la lista</a>
      </div>
    </div>
</form>

<form id="deleteForm" action="{{ path('linea_trabajo_delete', { 'id': entity.id }) }}" method="POST">
    {{ form_widget(delete_form) }}
</form> 
{% endblock body %}
{% block javascripts %}
{{ parent() }}
<script> 
    (function($) {

        $('#js-delete').on('click', function(){
            $('#deleteForm').submit();
            return false;
        });
    })(jQuery);
</script>
{% endblock %}